export interface ServiciosCardProps {
  servicio: {
    imgUrl: string;
    id: number;
    title: string;
  };
}

export interface OfrecemosCards {
    card: {
        id: number;
        text: string;
    }
}